# Fase 1 — Cloud (auth, sync, invito via link, presenza, cronologia) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere sync cloud opzionale per viaggio (Supabase), invito via link, indicatore di stato, presenza per singola voce e cronologia minima a un livello, senza rompere il funzionamento local-first esistente.

**Architecture:** Livello di sync pull-based (nessun realtime) sovrapposto ai viaggi locali esistenti. Il documento viaggio (`schema.js`) guadagna due campi opzionali di attribuzione per nodo editabile; lo stato di sync (chi è il viaggio remoto, dirty o non) vive separato in IndexedDB, non nel documento viaggio. Due nuove viste (`ActivateSyncModal`, `JoinView`) e due nuovi moduli dati (`supabase.js`, `sync.js`) orchestrano auth e sync; il resto dell'app resta invariato quando la sync non è attiva.

**Tech Stack:** React 18, Vite, Tailwind, `@supabase/supabase-js` (nuova dipendenza, nel budget CLAUDE.md), `idb-keyval`, `vitest` (nuova devDependency, approvata in questa sessione per testare la logica pura di sync/schema).

**Spec:** [docs/superpowers/specs/2026-08-18-fase1-cloud-design.md](../specs/2026-08-18-fase1-cloud-design.md)

## Global Constraints

- Dipendenze permesse: React, Tailwind, lucide-react, idb-keyval, `@supabase/supabase-js`, vite-plugin-pwa, più `vitest` (devDependency, approvata per questa fase). Nessun'altra dipendenza senza chiedere — niente React Testing Library, niente react-router-dom.
- Niente cartella `utils/` generica: le funzioni stanno vicino a chi le usa. I componenti condivisi (form di login, form del nome) vanno in `src/components/`, come `Btn`/`Modal`/`Label` esistenti.
- Un componente per file; niente file oltre ~250 righe.
- Copy in italiano, tono piano, seconda persona; i bottoni dicono cosa succede.
- Aree toccabili almeno 44px (`min-h-11`/`min-w-11`, pattern già in uso); focus visibile sempre.
- Solo i colori del tema attivo (`var(--accent)`, `var(--accent2)`, `var(--muted)`, ecc.) — niente colori Tailwind arbitrari fuori palette.
- Local-first: l'app deve restare interamente funzionante senza account e senza rete. Se `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` non sono configurate, le azioni cloud restano nascoste, nessuna schermata bloccante.
- Le chiavi Supabase stanno solo in `.env.local` (mai nel codice, mai nei commit; è già in `.gitignore`).
- Le migrazioni SQL si applicano dalla dashboard Supabase (mai stack locale via Docker, per il proxy aziendale).
- Solo tre tipi di sezione (`cards`, `checklist`, `notes`) — nessun tipo nuovo, nessun campo obbligatorio nuovo nel documento viaggio.
- Commit piccoli, messaggio in italiano all'imperativo.
- A fine piano: `npm run build` deve passare e va rifatta la verifica offline (`npm run preview`, DevTools → Network → Offline).

---

### Task 1: Aggiungi vitest come test runner

**Files:**
- Modify: `package.json`
- Modify: `vite.config.js`

**Interfaces:**
- Produces: comando `npm test` che esegue tutti i file `*.test.js` sotto `src/` con vitest.

- [ ] **Step 1: Installa vitest come devDependency**

```bash
npm install -D vitest
```

- [ ] **Step 2: Aggiungi lo script di test**

In `package.json`, dentro `"scripts"`:

```json
"test": "vitest run"
```

- [ ] **Step 3: Abilita il blocco test in vite.config.js**

In `vite.config.js`, aggiungi la proprietà `test` all'oggetto passato a `defineConfig` (accanto a `plugins`):

```js
export default defineConfig({
  plugins: [
    react(),
    VitePWA({ /* ... invariato ... */ })
  ],
  test: {}
})
```

- [ ] **Step 4: Verifica che il comando funzioni**

Run: `npm test`
Expected: esce con codice 0, output tipo "No test files found" (nessun test esiste ancora — è corretto).

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json vite.config.js
git commit -m "aggiungi vitest per testare la logica di sync"
```

---

### Task 2: Attribuzione per voce in `schema.js`

**Files:**
- Modify: `src/data/schema.js`
- Test: `src/data/schema.test.js` (nuovo)

**Interfaces:**
- Produces: ogni `Day`, `DayItem`, `CardItem`, `ChecklistItem` e la sezione `notes` hanno ora i campi stringa `modifiedBy` e `modifiedAt`, normalizzati come tutti gli altri campi (stringa vuota se assenti) ed esportati come tutti gli altri. Nuova funzione esportata `stampModified(node, displayName)`.

- [ ] **Step 1: Scrivi i test che falliscono**

Crea `src/data/schema.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { normalizeTrip, exportTrip, stampModified } from './schema.js'

describe('normalizeTrip — attribuzione', () => {
  it('riempie modifiedBy/modifiedAt vuoti quando assenti', () => {
    const trip = normalizeTrip({
      name: 'Ponza',
      days: [{ date: '2026-08-30', items: [{ title: 'Aliscafo' }] }],
      sections: [
        { title: 'Ristoranti', type: 'cards', items: [{ title: 'Trattoria' }] },
        { title: 'Da fare', type: 'checklist', items: [{ text: 'Prenotare' }] },
        { title: 'Note', type: 'notes', text: 'ricordati la crema solare' }
      ]
    })

    expect(trip.days[0].modifiedBy).toBe('')
    expect(trip.days[0].modifiedAt).toBe('')
    expect(trip.days[0].items[0].modifiedBy).toBe('')
    expect(trip.sections[0].items[0].modifiedBy).toBe('')
    expect(trip.sections[1].items[0].modifiedBy).toBe('')
    expect(trip.sections[2].modifiedBy).toBe('')
  })

  it('preserva modifiedBy/modifiedAt quando presenti', () => {
    const trip = normalizeTrip({
      name: 'Ponza',
      days: [{ date: '2026-08-30', modifiedBy: 'Vincenzo', modifiedAt: '2026-08-18T10:00:00.000Z', items: [] }]
    })
    expect(trip.days[0].modifiedBy).toBe('Vincenzo')
    expect(trip.days[0].modifiedAt).toBe('2026-08-18T10:00:00.000Z')
  })
})

describe('exportTrip — attribuzione', () => {
  it('esporta modifiedBy/modifiedAt su giorni, voci e sezione notes', () => {
    const trip = normalizeTrip({
      name: 'Ponza',
      days: [{ date: '2026-08-30', modifiedBy: 'Vincenzo', modifiedAt: 't1', items: [{ title: 'Aliscafo', modifiedBy: 'Giulia', modifiedAt: 't2' }] }],
      sections: [{ title: 'Note', type: 'notes', text: 'x', modifiedBy: 'Giulia', modifiedAt: 't3' }]
    })
    const exported = exportTrip(trip)
    expect(exported.days[0].modifiedBy).toBe('Vincenzo')
    expect(exported.days[0].items[0].modifiedBy).toBe('Giulia')
    expect(exported.sections[0].modifiedBy).toBe('Giulia')
  })
})

describe('stampModified', () => {
  it('non modifica il nodo se non c\'è un nome (viaggio non attivo in sync)', () => {
    const node = { title: 'x' }
    expect(stampModified(node, '')).toBe(node)
  })

  it('aggiunge modifiedBy e modifiedAt (ISO) se c\'è un nome', () => {
    const node = { title: 'x' }
    const result = stampModified(node, 'Vincenzo')
    expect(result.modifiedBy).toBe('Vincenzo')
    expect(new Date(result.modifiedAt).toString()).not.toBe('Invalid Date')
    expect(result).not.toBe(node)
  })
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npm test`
Expected: FAIL — `modifiedBy` è `undefined` invece di `''`, e `stampModified` non è esportato.

- [ ] **Step 3: Implementa in `schema.js`**

Modifica le funzioni di normalizzazione per aggiungere i due campi. Sostituisci il corpo di `normalizeDayItem`, `normalizeDay`, `normalizeCardItem`, `normalizeChecklistItem` e il ramo `notes` di `normalizeSection` così:

```js
function normalizeDayItem(raw) {
  const item = raw && typeof raw === 'object' ? raw : {}
  return {
    id: makeId(),
    time: str(item.time),
    title: str(item.title),
    detail: str(item.detail),
    link: str(item.link),
    modifiedBy: str(item.modifiedBy),
    modifiedAt: str(item.modifiedAt)
  }
}

function normalizeDay(raw) {
  const day = raw && typeof raw === 'object' ? raw : {}
  return {
    id: makeId(),
    date: str(day.date),
    title: str(day.title),
    note: str(day.note),
    modifiedBy: str(day.modifiedBy),
    modifiedAt: str(day.modifiedAt),
    items: arr(day.items).map(normalizeDayItem)
  }
}

function normalizeCardItem(raw) {
  const item = raw && typeof raw === 'object' ? raw : {}
  return {
    id: makeId(),
    title: str(item.title),
    meta: str(item.meta),
    detail: str(item.detail),
    link: str(item.link),
    tags: arr(item.tags).map(str),
    modifiedBy: str(item.modifiedBy),
    modifiedAt: str(item.modifiedAt)
  }
}

function normalizeChecklistItem(raw) {
  const item = raw && typeof raw === 'object' ? raw : {}
  return {
    id: makeId(),
    text: str(item.text),
    done: item.done === true,
    modifiedBy: str(item.modifiedBy),
    modifiedAt: str(item.modifiedAt)
  }
}
```

Nel ramo `notes` di `normalizeSection`:

```js
  if (type === 'notes') {
    return { ...base, text: str(section.text), modifiedBy: str(section.modifiedBy), modifiedAt: str(section.modifiedAt) }
  }
```

- [ ] **Step 4: Aggiorna `exportTrip` per includere i campi a livello giorno e sezione notes**

`days.map` esporta anche `modifiedBy`/`modifiedAt` del giorno (gli item lo hanno già via `withoutId` che passa tutti i campi restanti):

```js
    days: trip.days.map((day) => ({
      date: day.date,
      title: day.title,
      note: day.note,
      modifiedBy: day.modifiedBy,
      modifiedAt: day.modifiedAt,
      items: day.items.map(withoutId)
    })),
```

Nel ramo `notes` di `sections.map`:

```js
      if (section.type === 'notes') {
        return { ...base, text: section.text, modifiedBy: section.modifiedBy, modifiedAt: section.modifiedAt }
      }
```

- [ ] **Step 5: Aggiungi `stampModified`**

In fondo a `schema.js`, dopo `exportTrip`:

```js
// Usata dalle viste quando si modifica una voce di un viaggio con sync attiva:
// se non c'è un nome (viaggio non attivo o dispositivo senza preferenza) il
// nodo torna invariato, così l'attribuzione non appare mai per un viaggio
// puramente locale.
export function stampModified(node, displayName) {
  if (!displayName) return node
  return { ...node, modifiedBy: displayName, modifiedAt: new Date().toISOString() }
}
```

- [ ] **Step 6: Esegui i test e verifica che passino**

Run: `npm test`
Expected: PASS, tutti i test verdi.

- [ ] **Step 7: Commit**

```bash
git add src/data/schema.js src/data/schema.test.js
git commit -m "aggiungi attribuzione modifiedBy/modifiedAt allo schema del viaggio"
```

---

### Task 3: Motore di decisione sync (funzione pura)

**Files:**
- Create: `src/data/sync.js`
- Test: `src/data/sync.test.js` (nuovo)

**Interfaces:**
- Produces: `decideSyncAction({ dirty, lastSyncedAt, remoteUpdatedAt }) → 'noop' | 'pull' | 'push' | 'conflict'`, `generateShareCode() → string` (6 caratteri).

- [ ] **Step 1: Scrivi i test che falliscono**

Crea `src/data/sync.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { decideSyncAction, generateShareCode } from './sync.js'

describe('decideSyncAction', () => {
  it('noop se non dirty e il remoto non è più recente', () => {
    expect(decideSyncAction({ dirty: false, lastSyncedAt: '2026-08-18T10:00:00Z', remoteUpdatedAt: '2026-08-18T10:00:00Z' })).toBe('noop')
  })

  it('noop se non dirty e non c\'è ancora nulla di remoto', () => {
    expect(decideSyncAction({ dirty: false, lastSyncedAt: null, remoteUpdatedAt: null })).toBe('noop')
  })

  it('pull se non dirty e il remoto è più recente', () => {
    expect(decideSyncAction({ dirty: false, lastSyncedAt: '2026-08-18T10:00:00Z', remoteUpdatedAt: '2026-08-18T11:00:00Z' })).toBe('pull')
  })

  it('push se dirty e il remoto non è cambiato dall\'ultimo sync', () => {
    expect(decideSyncAction({ dirty: true, lastSyncedAt: '2026-08-18T10:00:00Z', remoteUpdatedAt: '2026-08-18T10:00:00Z' })).toBe('push')
  })

  it('push alla primissima attivazione (nessun lastSyncedAt, nessun remoteUpdatedAt noto)', () => {
    expect(decideSyncAction({ dirty: true, lastSyncedAt: null, remoteUpdatedAt: null })).toBe('push')
  })

  it('conflict se dirty e il remoto è cambiato dall\'ultimo sync', () => {
    expect(decideSyncAction({ dirty: true, lastSyncedAt: '2026-08-18T10:00:00Z', remoteUpdatedAt: '2026-08-18T11:00:00Z' })).toBe('conflict')
  })
})

describe('generateShareCode', () => {
  it('genera un codice di 6 caratteri dal set consentito', () => {
    const code = generateShareCode()
    expect(code).toHaveLength(6)
    expect(code).toMatch(/^[ABCDEFGHJKLMNPQRSTUVWXYZ23456789]{6}$/)
  })
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npm test`
Expected: FAIL — il file `src/data/sync.js` non esiste ancora.

- [ ] **Step 3: Implementa `decideSyncAction` e `generateShareCode`**

Crea `src/data/sync.js`:

```js
export function decideSyncAction({ dirty, lastSyncedAt, remoteUpdatedAt }) {
  const remoteIsNewer = Boolean(remoteUpdatedAt) && (!lastSyncedAt || remoteUpdatedAt > lastSyncedAt)
  if (!dirty) {
    return remoteIsNewer ? 'pull' : 'noop'
  }
  return remoteIsNewer ? 'conflict' : 'push'
}

const SHARE_CODE_CHARS = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'

export function generateShareCode() {
  let code = ''
  for (let i = 0; i < 6; i++) {
    code += SHARE_CODE_CHARS[Math.floor(Math.random() * SHARE_CODE_CHARS.length)]
  }
  return code
}
```

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/data/sync.js src/data/sync.test.js
git commit -m "aggiungi la logica pura di decisione della sync"
```

---

### Task 4: Componente di presenza `ModifiedBy`

**Files:**
- Create: `src/components/ModifiedBy.jsx`
- Test: `src/components/ModifiedBy.test.js` (nuovo)

**Interfaces:**
- Consumes: nessuno (componente foglia).
- Produces: `formatRelativeTime(isoString, now = new Date()) → string`; componente default `ModifiedBy({ modifiedBy, modifiedAt })` che renderizza `null` se uno dei due è vuoto.

- [ ] **Step 1: Scrivi i test che falliscono**

Crea `src/components/ModifiedBy.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { formatRelativeTime } from './ModifiedBy.jsx'

describe('formatRelativeTime', () => {
  const now = new Date('2026-08-18T12:00:00.000Z')

  it('minuti fa', () => {
    const then = new Date('2026-08-18T11:58:00.000Z')
    expect(formatRelativeTime(then.toISOString(), now)).toContain('minuti fa')
  })

  it('un\'ora fa', () => {
    const then = new Date('2026-08-18T11:00:00.000Z')
    expect(formatRelativeTime(then.toISOString(), now)).toContain('ora fa')
  })

  it('giorni fa', () => {
    const then = new Date('2026-08-15T12:00:00.000Z')
    expect(formatRelativeTime(then.toISOString(), now)).toContain('giorni fa')
  })
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npm test`
Expected: FAIL — il file `src/components/ModifiedBy.jsx` non esiste ancora.

- [ ] **Step 3: Implementa il componente**

Crea `src/components/ModifiedBy.jsx`:

```jsx
const UNITS = [
  { limit: 60, divisor: 1, unit: 'second' },
  { limit: 3600, divisor: 60, unit: 'minute' },
  { limit: 86400, divisor: 3600, unit: 'hour' },
  { limit: Infinity, divisor: 86400, unit: 'day' }
]

const RTF = new Intl.RelativeTimeFormat('it', { numeric: 'auto' })

export function formatRelativeTime(isoString, now = new Date()) {
  const then = new Date(isoString)
  const diffSeconds = Math.max(0, Math.round((now - then) / 1000))
  const { divisor, unit } = UNITS.find((u) => diffSeconds < u.limit)
  return RTF.format(-Math.round(diffSeconds / divisor), unit)
}

export default function ModifiedBy({ modifiedBy, modifiedAt }) {
  if (!modifiedBy || !modifiedAt) return null
  return (
    <p className="font-mono text-[11px] text-[var(--muted)] mt-0.5">
      modificato da {modifiedBy} · {formatRelativeTime(modifiedAt)}
    </p>
  )
}
```

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/ModifiedBy.jsx src/components/ModifiedBy.test.js
git commit -m "aggiungi il componente di presenza ModifiedBy"
```

---

### Task 5: Bookkeeping locale della sync in `storage.js`

**Files:**
- Modify: `src/data/storage.js`
- Test: `src/data/storage.test.js` (nuovo)

**Interfaces:**
- Consumes: `get`/`set` da `idb-keyval` (già usati).
- Produces: `getSyncState(localTripId)`, `setSyncState(localTripId, state)`, `markDirty(localTripId)`, `getDisplayNamePreference()`, `setDisplayNamePreference(name)`.

- [ ] **Step 1: Scrivi i test che falliscono**

Crea `src/data/storage.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('idb-keyval', () => ({ get: vi.fn(), set: vi.fn() }))
vi.mock('./seed.js', () => ({ loadSeedTrips: vi.fn() }))

import { get, set } from 'idb-keyval'
import { getSyncState, setSyncState, markDirty, getDisplayNamePreference, setDisplayNamePreference } from './storage.js'

beforeEach(() => {
  get.mockReset()
  set.mockReset()
})

describe('getSyncState / setSyncState', () => {
  it('legge con la chiave wego:sync:<id> e torna null se assente', async () => {
    get.mockResolvedValue(undefined)
    const result = await getSyncState('trip-1')
    expect(get).toHaveBeenCalledWith('wego:sync:trip-1')
    expect(result).toBeNull()
  })

  it('scrive con la chiave wego:sync:<id>', async () => {
    const state = { remoteId: 'r1', role: 'editor', lastSyncedAt: null, dirty: true }
    await setSyncState('trip-1', state)
    expect(set).toHaveBeenCalledWith('wego:sync:trip-1', state)
  })
})

describe('markDirty', () => {
  it('non fa nulla se il viaggio non è attivato', async () => {
    get.mockResolvedValue(undefined)
    await markDirty('trip-1')
    expect(set).not.toHaveBeenCalled()
  })

  it('imposta dirty=true preservando il resto dello stato', async () => {
    get.mockResolvedValue({ remoteId: 'r1', role: 'editor', lastSyncedAt: 't0', dirty: false })
    await markDirty('trip-1')
    expect(set).toHaveBeenCalledWith('wego:sync:trip-1', { remoteId: 'r1', role: 'editor', lastSyncedAt: 't0', dirty: true })
  })
})

describe('preferenza nome dispositivo', () => {
  it('torna stringa vuota se non impostata', async () => {
    get.mockResolvedValue(undefined)
    expect(await getDisplayNamePreference()).toBe('')
  })

  it('scrive con la chiave wego:display-name', async () => {
    await setDisplayNamePreference('Vincenzo')
    expect(set).toHaveBeenCalledWith('wego:display-name', 'Vincenzo')
  })
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npm test`
Expected: FAIL — le nuove funzioni non esistono ancora in `storage.js`.

- [ ] **Step 3: Implementa in `storage.js`**

Aggiungi in fondo a `src/data/storage.js`:

```js
const SYNC_PREFIX = 'wego:sync:'
const DISPLAY_NAME_KEY = 'wego:display-name'

export async function getSyncState(localTripId) {
  const state = await get(SYNC_PREFIX + localTripId)
  return state ?? null
}

export async function setSyncState(localTripId, state) {
  await set(SYNC_PREFIX + localTripId, state)
}

export async function markDirty(localTripId) {
  const state = await getSyncState(localTripId)
  if (!state) return
  await setSyncState(localTripId, { ...state, dirty: true })
}

export async function getDisplayNamePreference() {
  const name = await get(DISPLAY_NAME_KEY)
  return name ?? ''
}

export async function setDisplayNamePreference(name) {
  await set(DISPLAY_NAME_KEY, name)
}
```

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/data/storage.js src/data/storage.test.js
git commit -m "aggiungi il bookkeeping locale della sync in storage.js"
```

---

### Task 6: Client Supabase e helper di autenticazione

**Files:**
- Create: `src/data/supabase.js`
- Create: `.env.local.example`
- Test: `src/data/supabase.test.js` (nuovo)

**Interfaces:**
- Consumes: `createClient` da `@supabase/supabase-js`.
- Produces: `computeIsCloudConfigured(url, anonKey) → boolean`, `isCloudConfigured` (boolean), `supabase` (client o `null`), `sendMagicLink(email)`, `subscribeAuth(callback) → unsubscribe`, `getSession() → Promise<session|null>`.

- [ ] **Step 1: Installa la dipendenza**

```bash
npm install @supabase/supabase-js
```

- [ ] **Step 2: Scrivi il test che fallisce**

Crea `src/data/supabase.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { computeIsCloudConfigured } from './supabase.js'

describe('computeIsCloudConfigured', () => {
  it('falso se manca l\'url', () => {
    expect(computeIsCloudConfigured('', 'anon-key')).toBe(false)
  })

  it('falso se manca la chiave anonima', () => {
    expect(computeIsCloudConfigured('https://x.supabase.co', '')).toBe(false)
  })

  it('vero se entrambi presenti', () => {
    expect(computeIsCloudConfigured('https://x.supabase.co', 'anon-key')).toBe(true)
  })
})
```

- [ ] **Step 3: Esegui il test e verifica che fallisca**

Run: `npm test`
Expected: FAIL — `src/data/supabase.js` non esiste ancora.

- [ ] **Step 4: Implementa `supabase.js`**

Crea `src/data/supabase.js`:

```js
import { createClient } from '@supabase/supabase-js'

export function computeIsCloudConfigured(url, anonKey) {
  return Boolean(url && anonKey)
}

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY

export const isCloudConfigured = computeIsCloudConfigured(SUPABASE_URL, SUPABASE_ANON_KEY)
export const supabase = isCloudConfigured ? createClient(SUPABASE_URL, SUPABASE_ANON_KEY) : null

export async function sendMagicLink(email) {
  const { error } = await supabase.auth.signInWithOtp({ email })
  if (error) throw new Error(error.message)
}

export function subscribeAuth(callback) {
  const { data } = supabase.auth.onAuthStateChange((_event, session) => callback(session))
  return () => data.subscription.unsubscribe()
}

export async function getSession() {
  const { data } = await supabase.auth.getSession()
  return data.session
}
```

- [ ] **Step 5: Esegui il test e verifica che passi**

Run: `npm test`
Expected: PASS.

- [ ] **Step 6: Documenta le variabili d'ambiente**

Crea `.env.local.example` (nessun valore reale, solo i nomi già decisi in CLAUDE.md):

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json src/data/supabase.js src/data/supabase.test.js .env.local.example
git commit -m "aggiungi il client Supabase e gli helper di autenticazione"
```

---

### Task 7: Migrazione SQL — tabelle, RLS, RPC `join_trip`

**Files:**
- Create: `supabase/sql/0001_cloud_schema.sql`

**Interfaces:**
- Produces: tabelle `tv_trips` (con `previous_data`) e `tv_trip_members` (con `display_name`), RLS, funzione `join_trip(code, display_name) → uuid`.

Nessun test automatico: è SQL da eseguire manualmente sulla dashboard Supabase (niente stack locale, per il proxy aziendale). Il task è "fatto" quando lo script è stato eseguito con successo sul progetto `yloymohhoigvcchtexdb` e le tabelle esistono.

- [ ] **Step 1: Scrivi la migrazione**

Crea `supabase/sql/0001_cloud_schema.sql`:

```sql
create table if not exists tv_trips (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id),
  share_code text not null unique,
  data jsonb not null,
  previous_data jsonb,
  updated_at timestamptz not null default now()
);

create table if not exists tv_trip_members (
  trip_id uuid references tv_trips(id) on delete cascade,
  user_id uuid references auth.users(id),
  role text not null default 'viewer',
  display_name text,
  primary key (trip_id, user_id)
);

alter table tv_trips enable row level security;
alter table tv_trip_members enable row level security;

drop policy if exists "tv_trips_select" on tv_trips;
create policy "tv_trips_select" on tv_trips for select
  using (
    owner_id = auth.uid()
    or exists (select 1 from tv_trip_members m where m.trip_id = tv_trips.id and m.user_id = auth.uid())
  );

drop policy if exists "tv_trips_insert" on tv_trips;
create policy "tv_trips_insert" on tv_trips for insert
  with check (owner_id = auth.uid());

drop policy if exists "tv_trips_update" on tv_trips;
create policy "tv_trips_update" on tv_trips for update
  using (
    owner_id = auth.uid()
    or exists (select 1 from tv_trip_members m where m.trip_id = tv_trips.id and m.user_id = auth.uid() and m.role = 'editor')
  );

drop policy if exists "tv_trip_members_select" on tv_trip_members;
create policy "tv_trip_members_select" on tv_trip_members for select
  using (
    user_id = auth.uid()
    or exists (select 1 from tv_trips t where t.id = tv_trip_members.trip_id and t.owner_id = auth.uid())
  );

drop policy if exists "tv_trip_members_insert_self" on tv_trip_members;
create policy "tv_trip_members_insert_self" on tv_trip_members for insert
  with check (user_id = auth.uid());

create or replace function join_trip(code text, display_name text)
returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
  target_trip_id uuid;
begin
  select id into target_trip_id from tv_trips where share_code = code;
  if target_trip_id is null then
    raise exception 'Codice non valido';
  end if;

  insert into tv_trip_members (trip_id, user_id, role, display_name)
  values (target_trip_id, auth.uid(), 'viewer', display_name)
  on conflict (trip_id, user_id) do update set display_name = excluded.display_name;

  return target_trip_id;
end;
$$;
```

- [ ] **Step 2: Applica la migrazione dalla dashboard**

Apri il progetto Supabase `yloymohhoigvcchtexdb` → SQL Editor → incolla il contenuto del file → esegui. Verifica in Table Editor che `tv_trips` e `tv_trip_members` esistano con le colonne previste.

- [ ] **Step 3: Compila `.env.local`**

Copia `.env.local.example` in `.env.local` e inserisci l'URL del progetto e la anon key (dalla dashboard Supabase → Project Settings → API). Non committare questo file (già in `.gitignore`).

- [ ] **Step 4: Commit (solo il file SQL, non `.env.local`)**

```bash
git add supabase/sql/0001_cloud_schema.sql
git commit -m "aggiungi la migrazione per tabelle cloud, RLS e join_trip"
```

---

### Task 8: Attivazione sync (`activateTripSync`) e pull/push di base

**Files:**
- Modify: `src/data/sync.js`
- Test: `src/data/sync.test.js`

**Interfaces:**
- Consumes: `supabase`, `getSession` da `./supabase.js`; `exportTrip`, `normalizeTrip` da `./schema.js`; `generateShareCode` (già in questo file).
- Produces: `activateTripSync(trip, displayName) → Promise<{ remoteId, shareCode, lastSyncedAt, role: 'editor', dirty: false }>`, `pullTrip(syncState) → Promise<{ trip, syncState }>`, `pushTrip(trip, syncState) → Promise<{ trip, syncState }>`.

- [ ] **Step 1: Scrivi i test che falliscono**

Aggiungi a `src/data/sync.test.js`:

```js
import { vi, beforeEach } from 'vitest'

const { mockFrom, mockGetSession } = vi.hoisted(() => ({ mockFrom: vi.fn(), mockGetSession: vi.fn() }))

vi.mock('./supabase.js', () => ({ supabase: { from: mockFrom }, getSession: mockGetSession }))

const { activateTripSync, pullTrip, pushTrip } = await import('./sync.js')
const { normalizeTrip } = await import('./schema.js')

beforeEach(() => {
  mockFrom.mockReset()
  mockGetSession.mockReset()
})

describe('activateTripSync', () => {
  it('crea la riga tv_trips e la membership owner, torna remoteId/shareCode', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'user-1' } })
    const insertTrip = vi.fn().mockReturnValue({
      select: () => ({ single: async () => ({ data: { id: 'trip-remote-1', share_code: 'AB12CD', updated_at: '2026-08-18T10:00:00Z' }, error: null }) })
    })
    const insertMember = vi.fn().mockResolvedValue({ error: null })
    mockFrom.mockImplementation((table) => {
      if (table === 'tv_trips') return { insert: insertTrip }
      if (table === 'tv_trip_members') return { insert: insertMember }
      throw new Error(`tabella inattesa: ${table}`)
    })

    const trip = normalizeTrip({ name: 'Ponza' })
    const result = await activateTripSync(trip, 'Vincenzo')

    expect(result).toEqual({ remoteId: 'trip-remote-1', shareCode: 'AB12CD', lastSyncedAt: '2026-08-18T10:00:00Z', role: 'editor', dirty: false })
    expect(insertMember).toHaveBeenCalledWith({ trip_id: 'trip-remote-1', user_id: 'user-1', role: 'editor', display_name: 'Vincenzo' })
  })

  it('rifiuta se non c\'è una sessione', async () => {
    mockGetSession.mockResolvedValue(null)
    const trip = normalizeTrip({ name: 'Ponza' })
    await expect(activateTripSync(trip, 'Vincenzo')).rejects.toThrow()
  })
})

describe('pullTrip', () => {
  it('normalizza il viaggio remoto e aggiorna lastSyncedAt', async () => {
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { data: { name: 'Ponza' }, updated_at: '2026-08-18T12:00:00Z' }, error: null }) }) })
    })
    const syncState = { remoteId: 'trip-remote-1', role: 'viewer', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: false }
    const result = await pullTrip(syncState)
    expect(result.trip.name).toBe('Ponza')
    expect(result.syncState).toEqual({ ...syncState, lastSyncedAt: '2026-08-18T12:00:00Z', dirty: false })
  })
})

describe('pushTrip', () => {
  it('aggiorna data, sposta il vecchio valore in previous_data, aggiorna lastSyncedAt', async () => {
    const trip = normalizeTrip({ name: 'Ponza aggiornata' })
    const updateFn = vi.fn().mockReturnValue({
      eq: () => ({ select: () => ({ single: async () => ({ data: { updated_at: '2026-08-18T13:00:00Z' }, error: null }) }) })
    })
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { data: { name: 'Ponza vecchia' } }, error: null }) }) }),
      update: updateFn
    })
    const syncState = { remoteId: 'trip-remote-1', role: 'editor', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: true }
    const result = await pushTrip(trip, syncState)
    expect(result.syncState).toEqual({ ...syncState, lastSyncedAt: '2026-08-18T13:00:00Z', dirty: false })
    expect(updateFn).toHaveBeenCalledWith(expect.objectContaining({ previous_data: { name: 'Ponza vecchia' } }))
  })
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npm test`
Expected: FAIL — `activateTripSync`, `pullTrip`, `pushTrip` non esistono ancora.

- [ ] **Step 3: Implementa in `sync.js`**

Aggiungi in cima al file gli import necessari e in fondo le tre funzioni:

```js
import { supabase, getSession } from './supabase.js'
import { exportTrip, normalizeTrip } from './schema.js'
```

```js
export async function activateTripSync(trip, displayName) {
  const session = await getSession()
  if (!session) throw new Error('Devi accedere prima di attivare la sincronizzazione.')

  const payload = exportTrip(trip)
  let lastError = null
  for (let attempt = 0; attempt < 3; attempt++) {
    const shareCode = generateShareCode()
    const { data, error } = await supabase
      .from('tv_trips')
      .insert({ owner_id: session.user.id, share_code: shareCode, data: payload })
      .select('id, share_code, updated_at')
      .single()

    if (!error) {
      const { error: memberError } = await supabase
        .from('tv_trip_members')
        .insert({ trip_id: data.id, user_id: session.user.id, role: 'editor', display_name: displayName })
      if (memberError) throw new Error(memberError.message)
      return { remoteId: data.id, shareCode: data.share_code, lastSyncedAt: data.updated_at, role: 'editor', dirty: false }
    }
    lastError = error
    if (error.code !== '23505') break
  }
  throw new Error(lastError?.message || 'Impossibile attivare la sincronizzazione.')
}

export async function pullTrip(syncState) {
  const { data: row, error } = await supabase
    .from('tv_trips')
    .select('data, updated_at')
    .eq('id', syncState.remoteId)
    .single()
  if (error) throw new Error(error.message)
  return {
    trip: normalizeTrip(row.data),
    syncState: { ...syncState, lastSyncedAt: row.updated_at, dirty: false }
  }
}

export async function pushTrip(trip, syncState) {
  const { data: current, error: readError } = await supabase
    .from('tv_trips')
    .select('data')
    .eq('id', syncState.remoteId)
    .single()
  if (readError) throw new Error(readError.message)

  const { data: updated, error } = await supabase
    .from('tv_trips')
    .update({ data: exportTrip(trip), previous_data: current.data, updated_at: new Date().toISOString() })
    .eq('id', syncState.remoteId)
    .select('updated_at')
    .single()
  if (error) throw new Error(error.message)

  return {
    trip,
    syncState: { ...syncState, lastSyncedAt: updated.updated_at, dirty: false }
  }
}
```

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/data/sync.js src/data/sync.test.js
git commit -m "aggiungi attivazione sync, pull e push di base"
```

---

### Task 9: Join via codice e orchestrazione completa della sync

**Files:**
- Modify: `src/data/sync.js`
- Test: `src/data/sync.test.js`

**Interfaces:**
- Consumes: `decideSyncAction`, `pullTrip`, `pushTrip` (Task 3 e 8).
- Produces: `joinTripByCode(code, displayName) → Promise<{ trip, syncState }>`, `syncTrip(trip, syncState) → Promise<{ action, trip, syncState, conflict? }>`, `restoreLastVersion(remoteId) → Promise<trip|null>`.

- [ ] **Step 1: Scrivi i test che falliscono**

Aggiungi a `src/data/sync.test.js`:

```js
describe('joinTripByCode', () => {
  it('chiama la RPC join_trip poi legge il viaggio, torna ruolo viewer', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'user-2' } })
    const rpc = vi.fn().mockResolvedValue({ data: 'trip-remote-1', error: null })
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { id: 'trip-remote-1', data: { name: 'Ponza' }, updated_at: '2026-08-18T10:00:00Z' }, error: null }) }) })
    })
    const result = await joinTripByCode('AB12CD', 'Giulia', rpc)
    expect(rpc).toHaveBeenCalledWith('join_trip', { code: 'AB12CD', display_name: 'Giulia' })
    expect(result.trip.name).toBe('Ponza')
    expect(result.syncState).toEqual({ remoteId: 'trip-remote-1', role: 'viewer', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: false })
  })
})

describe('syncTrip', () => {
  it('torna noop se non c\'è syncState', async () => {
    const trip = normalizeTrip({ name: 'Ponza' })
    const result = await syncTrip(trip, null)
    expect(result).toEqual({ action: 'noop', trip, syncState: null })
  })

  it('pull quando non dirty e il remoto è più recente', async () => {
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { data: { name: 'Ponza remota' }, updated_at: '2026-08-18T12:00:00Z' }, error: null }) }) })
    })
    const trip = normalizeTrip({ name: 'Ponza locale' })
    const syncState = { remoteId: 'trip-remote-1', role: 'viewer', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: false }
    const result = await syncTrip(trip, syncState)
    expect(result.action).toBe('pull')
    expect(result.trip.name).toBe('Ponza remota')
  })

  it('salta il push se il ruolo è viewer', async () => {
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { updated_at: '2026-08-18T10:00:00Z' }, error: null }) }) })
    })
    const trip = normalizeTrip({ name: 'Ponza locale' })
    const syncState = { remoteId: 'trip-remote-1', role: 'viewer', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: true }
    const result = await syncTrip(trip, syncState)
    expect(result.action).toBe('skipped-viewer')
  })

  it('conflict quando dirty e il remoto è cambiato, non sovrascrive nulla', async () => {
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { data: { name: 'Ponza remota' }, updated_at: '2026-08-18T13:00:00Z' }, error: null }) }) })
    })
    const trip = normalizeTrip({ name: 'Ponza locale' })
    const syncState = { remoteId: 'trip-remote-1', role: 'editor', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: true }
    const result = await syncTrip(trip, syncState)
    expect(result.action).toBe('conflict')
    expect(result.trip.name).toBe('Ponza locale')
    expect(result.conflict.remoteTrip.name).toBe('Ponza remota')
  })
})

describe('restoreLastVersion', () => {
  it('applica previous_data e lo azzera sul remoto, torna null se non c\'è nulla da ripristinare', async () => {
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { previous_data: null }, error: null }) }) })
    })
    expect(await restoreLastVersion('trip-remote-1')).toBeNull()
  })

  it('ripristina quando previous_data esiste', async () => {
    const updateFn = vi.fn().mockReturnValue({ eq: async () => ({ error: null }) })
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { previous_data: { name: 'Ponza vecchia' } }, error: null }) }) }),
      update: updateFn
    })
    const restored = await restoreLastVersion('trip-remote-1')
    expect(restored.name).toBe('Ponza vecchia')
    expect(updateFn).toHaveBeenCalledWith({ data: { name: 'Ponza vecchia' }, previous_data: null, updated_at: expect.any(String) })
  })
})
```

Nota: la firma di `joinTripByCode` nel test passa un terzo argomento `rpc` finto — allinealo nell'implementazione (Step 3) accettando opzionalmente la funzione rpc per i test, oppure (più semplice) chiama `supabase.rpc` direttamente: aggiorna il mock di `mockFrom`/`supabase` per includere `rpc: rpc` nell'oggetto esportato da `vi.mock('./supabase.js', ...)`. Usa questa versione del mock invece (sostituisci il blocco `vi.mock` in cima al file):

```js
const { mockFrom, mockGetSession, mockRpc } = vi.hoisted(() => ({ mockFrom: vi.fn(), mockGetSession: vi.fn(), mockRpc: vi.fn() }))
vi.mock('./supabase.js', () => ({ supabase: { from: mockFrom, rpc: mockRpc }, getSession: mockGetSession }))
```

E nel test di `joinTripByCode`, usa `mockRpc` al posto del parametro `rpc`:

```js
describe('joinTripByCode', () => {
  it('chiama la RPC join_trip poi legge il viaggio, torna ruolo viewer', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'user-2' } })
    mockRpc.mockResolvedValue({ data: 'trip-remote-1', error: null })
    mockFrom.mockReturnValue({
      select: () => ({ eq: () => ({ single: async () => ({ data: { id: 'trip-remote-1', data: { name: 'Ponza' }, updated_at: '2026-08-18T10:00:00Z' }, error: null }) }) })
    })
    const result = await joinTripByCode('AB12CD', 'Giulia')
    expect(mockRpc).toHaveBeenCalledWith('join_trip', { code: 'AB12CD', display_name: 'Giulia' })
    expect(result.trip.name).toBe('Ponza')
    expect(result.syncState).toEqual({ remoteId: 'trip-remote-1', role: 'viewer', lastSyncedAt: '2026-08-18T10:00:00Z', dirty: false })
  })
})
```

Aggiorna anche il `beforeEach` per resettare `mockRpc`.

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npm test`
Expected: FAIL — `joinTripByCode`, `syncTrip`, `restoreLastVersion` non esistono ancora.

- [ ] **Step 3: Implementa in `sync.js`**

Aggiungi in fondo al file:

```js
export async function joinTripByCode(code, displayName) {
  const session = await getSession()
  if (!session) throw new Error('Devi accedere prima di unirti al viaggio.')

  const { data: remoteId, error: rpcError } = await supabase.rpc('join_trip', { code, display_name: displayName })
  if (rpcError) throw new Error(rpcError.message)

  const { data: row, error: selectError } = await supabase
    .from('tv_trips')
    .select('id, data, updated_at')
    .eq('id', remoteId)
    .single()
  if (selectError) throw new Error(selectError.message)

  return {
    trip: normalizeTrip(row.data),
    syncState: { remoteId: row.id, role: 'viewer', lastSyncedAt: row.updated_at, dirty: false }
  }
}

export async function syncTrip(trip, syncState) {
  if (!syncState) return { action: 'noop', trip, syncState }

  const { data: row, error } = await supabase
    .from('tv_trips')
    .select('updated_at')
    .eq('id', syncState.remoteId)
    .single()
  if (error) throw new Error(error.message)

  const action = decideSyncAction({
    dirty: syncState.dirty,
    lastSyncedAt: syncState.lastSyncedAt,
    remoteUpdatedAt: row.updated_at
  })

  if (action === 'noop') return { action, trip, syncState }

  if (action === 'pull') {
    const result = await pullTrip(syncState)
    return { action, ...result }
  }

  if (action === 'conflict') {
    const { data: full, error: fullError } = await supabase
      .from('tv_trips')
      .select('data, updated_at')
      .eq('id', syncState.remoteId)
      .single()
    if (fullError) throw new Error(fullError.message)
    return { action, trip, syncState, conflict: { remoteTrip: normalizeTrip(full.data), remoteUpdatedAt: full.updated_at } }
  }

  if (syncState.role === 'viewer') return { action: 'skipped-viewer', trip, syncState }

  const result = await pushTrip(trip, syncState)
  return { action, ...result }
}

export async function restoreLastVersion(remoteId) {
  const { data: row, error } = await supabase
    .from('tv_trips')
    .select('previous_data')
    .eq('id', remoteId)
    .single()
  if (error) throw new Error(error.message)
  if (!row.previous_data) return null

  const { error: updateError } = await supabase
    .from('tv_trips')
    .update({ data: row.previous_data, previous_data: null, updated_at: new Date().toISOString() })
    .eq('id', remoteId)
  if (updateError) throw new Error(updateError.message)

  return normalizeTrip(row.previous_data)
}
```

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npm test`
Expected: PASS, tutti i test di `sync.test.js` verdi.

- [ ] **Step 5: Commit**

```bash
git add src/data/sync.js src/data/sync.test.js
git commit -m "aggiungi join via codice, orchestrazione sync e ripristino ultima versione"
```

---

### Task 10: Form condivisi — email magic link e nome dispositivo

**Files:**
- Create: `src/components/MagicLinkForm.jsx`
- Create: `src/components/DisplayNameForm.jsx`

**Interfaces:**
- Consumes: `sendMagicLink` da `../data/supabase.js`.
- Produces: `MagicLinkForm()` (nessuna prop, gestisce da solo lo stato "inviato"); `DisplayNameForm({ initialValue, onSubmit })`.

Nessun test automatico (componenti React senza React Testing Library nel budget): verifica manuale al Task 12/13 quando vengono montati nei flussi reali.

- [ ] **Step 1: Crea `MagicLinkForm.jsx`**

```jsx
import { useState } from 'react'
import Btn from './Btn.jsx'
import { sendMagicLink } from '../data/supabase.js'

const inputClass = 'border border-[var(--line)] bg-[var(--card)] rounded-md px-3 py-2 text-sm'

export default function MagicLinkForm() {
  const [email, setEmail] = useState('')
  const [sent, setSent] = useState(false)
  const [error, setError] = useState('')

  async function handleSubmit(e) {
    e.preventDefault()
    setError('')
    try {
      await sendMagicLink(email)
      setSent(true)
    } catch (err) {
      setError(err.message)
    }
  }

  if (sent) {
    return <p className="text-sm">Controlla la posta di {email}: tocca il link che ti abbiamo mandato, poi torna qui.</p>
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-3">
      {error && <p className="text-sm text-[var(--accent)]">{error}</p>}
      <input
        required
        type="email"
        placeholder="La tua email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        className={inputClass}
      />
      <Btn type="submit">Invia il link di accesso</Btn>
    </form>
  )
}
```

- [ ] **Step 2: Crea `DisplayNameForm.jsx`**

```jsx
import { useState } from 'react'
import Btn from './Btn.jsx'

const inputClass = 'border border-[var(--line)] bg-[var(--card)] rounded-md px-3 py-2 text-sm'

export default function DisplayNameForm({ initialValue, onSubmit }) {
  const [name, setName] = useState(initialValue || '')

  function handleSubmit(e) {
    e.preventDefault()
    const trimmed = name.trim()
    if (!trimmed) return
    onSubmit(trimmed)
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-3">
      <input
        required
        placeholder="Come ti chiamiamo?"
        value={name}
        onChange={(e) => setName(e.target.value)}
        className={inputClass}
      />
      <Btn type="submit">Continua</Btn>
    </form>
  )
}
```

- [ ] **Step 3: Verifica che il build passi**

Run: `npm run build`
Expected: build senza errori (i componenti non sono ancora usati da nessuna vista, ma devono compilare).

- [ ] **Step 4: Commit**

```bash
git add src/components/MagicLinkForm.jsx src/components/DisplayNameForm.jsx
git commit -m "aggiungi i form condivisi per magic link e nome dispositivo"
```

---

### Task 11: `ActivateSyncModal` — attivazione sync lato owner

**Files:**
- Create: `src/views/ActivateSyncModal.jsx`

**Interfaces:**
- Consumes: `Modal`, `Btn` (esistenti); `MagicLinkForm`, `DisplayNameForm` (Task 10); `subscribeAuth`, `getSession` da `../data/supabase.js`; `activateTripSync` da `../data/sync.js`; `getDisplayNamePreference`, `setDisplayNamePreference` da `../data/storage.js`.
- Produces: `ActivateSyncModal({ open, trip, onClose, onActivated })` — `onActivated(syncState)` chiamato a fine flusso con lo stesso shape tornato da `activateTripSync`.

- [ ] **Step 1: Crea il file**

```jsx
import { useEffect, useState } from 'react'
import Modal from '../components/Modal.jsx'
import Btn from '../components/Btn.jsx'
import MagicLinkForm from '../components/MagicLinkForm.jsx'
import DisplayNameForm from '../components/DisplayNameForm.jsx'
import { subscribeAuth, getSession } from '../data/supabase.js'
import { activateTripSync } from '../data/sync.js'
import { getDisplayNamePreference, setDisplayNamePreference } from '../data/storage.js'

export default function ActivateSyncModal({ open, trip, onClose, onActivated }) {
  const [step, setStep] = useState('email')
  const [namePreference, setNamePreference] = useState('')
  const [shareLink, setShareLink] = useState('')
  const [error, setError] = useState('')

  useEffect(() => {
    if (!open) return
    getDisplayNamePreference().then(setNamePreference)
    getSession().then((session) => { if (session) setStep('name') })
    const unsubscribe = subscribeAuth((session) => { if (session) setStep('name') })
    return unsubscribe
  }, [open])

  async function handleName(name) {
    setError('')
    await setDisplayNamePreference(name)
    try {
      const state = await activateTripSync(trip, name)
      setShareLink(`${window.location.origin}/j/${state.shareCode}`)
      setStep('done')
      onActivated(state)
    } catch (e) {
      setError(e.message)
      setStep('name')
    }
  }

  function copyLink() {
    navigator.clipboard.writeText(shareLink)
  }

  function handleClose() {
    setStep('email')
    setError('')
    onClose()
  }

  return (
    <Modal open={open} title="Attiva la sincronizzazione" onClose={handleClose}>
      {step === 'email' && <MagicLinkForm />}
      {step === 'name' && (
        <>
          {error && <p className="text-sm text-[var(--accent)] mb-2">{error}</p>}
          <DisplayNameForm initialValue={namePreference} onSubmit={handleName} />
        </>
      )}
      {step === 'done' && (
        <div className="flex flex-col gap-3">
          <p className="text-sm">Il viaggio è ora sincronizzato. Condividi questo link con chi viene con te:</p>
          <p className="font-mono text-sm break-all bg-[var(--tint)] rounded-md p-3">{shareLink}</p>
          <Btn onClick={copyLink}>Copia link</Btn>
        </div>
      )}
    </Modal>
  )
}
```

- [ ] **Step 2: Verifica che il build passi**

Run: `npm run build`
Expected: build senza errori (componente creato ma non ancora montato da nessuna vista).

- [ ] **Step 3: Commit**

```bash
git add src/views/ActivateSyncModal.jsx
git commit -m "aggiungi la vista di attivazione sync per l'owner"
```

---

### Task 12: `JoinView` e routing manuale per `/j/:code`

**Files:**
- Create: `src/views/JoinView.jsx`
- Modify: `src/App.jsx`

**Interfaces:**
- Consumes: `MagicLinkForm`, `DisplayNameForm` (Task 10); `subscribeAuth`, `getSession` da `../data/supabase.js`; `joinTripByCode` da `../data/sync.js`; `getDisplayNamePreference`, `setDisplayNamePreference`, `setSyncState` da `../data/storage.js`.
- Produces: `JoinView({ code, onJoined, onCancel })` — `onJoined(trip)` chiamato con il viaggio normalizzato già salvato come stato di sync locale.

- [ ] **Step 1: Crea `JoinView.jsx`**

```jsx
import { useEffect, useState } from 'react'
import Btn from '../components/Btn.jsx'
import MagicLinkForm from '../components/MagicLinkForm.jsx'
import DisplayNameForm from '../components/DisplayNameForm.jsx'
import { subscribeAuth, getSession } from '../data/supabase.js'
import { joinTripByCode } from '../data/sync.js'
import { getDisplayNamePreference, setDisplayNamePreference, setSyncState } from '../data/storage.js'

export default function JoinView({ code, onJoined, onCancel }) {
  const [step, setStep] = useState('email')
  const [namePreference, setNamePreference] = useState('')
  const [error, setError] = useState('')

  useEffect(() => {
    getDisplayNamePreference().then(setNamePreference)
    getSession().then((session) => { if (session) setStep('name') })
    const unsubscribe = subscribeAuth((session) => { if (session) setStep('name') })
    return unsubscribe
  }, [])

  async function handleName(name) {
    setError('')
    await setDisplayNamePreference(name)
    try {
      const { trip, syncState } = await joinTripByCode(code, name)
      await setSyncState(trip.id, syncState)
      onJoined(trip)
    } catch (e) {
      setError(e.message)
      setStep('name')
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center px-5 font-sans">
      <div className="max-w-sm w-full flex flex-col gap-4">
        <h1 className="font-display text-2xl">Ti hanno invitato a un viaggio</h1>
        <p className="text-sm text-[var(--muted)]">Codice: {code}</p>
        {step === 'email' && <MagicLinkForm />}
        {step === 'name' && (
          <>
            {error && <p className="text-sm text-[var(--accent)]">{error}</p>}
            <DisplayNameForm initialValue={namePreference} onSubmit={handleName} />
          </>
        )}
        <Btn variant="ghost" onClick={onCancel}>Annulla</Btn>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Aggiungi il routing manuale in `App.jsx`**

Modifica `src/App.jsx`: aggiungi l'import di `JoinView` e lo stato `joinCode`, letto una volta sola dal `pathname` all'avvio:

```jsx
import JoinView from './views/JoinView.jsx'
```

Dentro `App()`, subito dopo `const [activeTripId, setActiveTripId] = useState(null)`:

```jsx
  const [joinCode] = useState(() => {
    const match = window.location.pathname.match(/^\/j\/([A-Za-z0-9]{6})$/)
    return match ? match[1] : null
  })
```

Aggiungi la funzione che completa il join (dopo `importTrips`):

```jsx
  function finishJoin(trip) {
    window.history.replaceState(null, '', '/')
    persist([...trips, trip])
    openTrip(trip.id)
  }

  function cancelJoin() {
    window.history.replaceState(null, '', '/')
    window.location.reload()
  }
```

Nel corpo del render, subito dopo il controllo `if (trips === null) { ... }`, aggiungi:

```jsx
  if (joinCode) {
    return <JoinView code={joinCode} onJoined={finishJoin} onCancel={cancelJoin} />
  }
```

- [ ] **Step 3: Verifica manuale**

Avvia `npm run dev`, apri `http://localhost:5173/j/AB12CD` nel browser: deve apparire `JoinView` con il codice "AB12CD" e il form email (non la home). Verifica che "Annulla" torni alla home pulita (`/`).

- [ ] **Step 4: Commit**

```bash
git add src/views/JoinView.jsx src/App.jsx
git commit -m "aggiungi la vista di join e il routing manuale per /j/:code"
```

---

### Task 13: Wiring in `TripView` — attivazione, stato sync, conflitti, ripristino

**Files:**
- Modify: `src/views/TripView.jsx`
- Modify: `src/views/Overview.jsx`

**Interfaces:**
- Consumes: `getSyncState`, `setSyncState`, `markDirty`, `getDisplayNamePreference` da `../data/storage.js`; `syncTrip`, `pushTrip`, `pullTrip`, `restoreLastVersion` da `../data/sync.js`; `ActivateSyncModal` (Task 11).
- Produces: `TripView` passa a `Overview`/`Days`/`Section` un `onUpdate` che marca automaticamente `dirty` quando il viaggio è attivo, e a `Days`/`Section` la prop `activeDisplayName` (stringa, vuota se il viaggio non è attivo in sync).

- [ ] **Step 1: Aggiungi stato e caricamento in `TripView.jsx`**

Aggiungi gli import necessari in cima al file:

```jsx
import { useEffect, useState } from 'react'
import { getSyncState, setSyncState as persistSyncState, markDirty, getDisplayNamePreference } from '../data/storage.js'
import { syncTrip, pushTrip, pullTrip, restoreLastVersion } from '../data/sync.js'
import ActivateSyncModal from './ActivateSyncModal.jsx'
import Modal from '../components/Modal.jsx'
import Btn from '../components/Btn.jsx'
```

(la riga `import { useState } from 'react'` esistente va sostituita da quella sopra che include anche `useEffect`).

Dentro `TripView({ trip, onBack, onUpdate, onDelete })`, dopo `const [activeTab, setActiveTab] = useState('overview')`:

```jsx
  const [syncState, setSyncStateValue] = useState(null)
  const [cloudDisplayName, setCloudDisplayName] = useState('')
  const [activateOpen, setActivateOpen] = useState(false)
  const [conflict, setConflict] = useState(null)

  async function runSync(state) {
    try {
      const result = await syncTrip(trip, state)
      if (result.action === 'conflict') {
        setConflict(result.conflict)
        return
      }
      if (result.action === 'pull') {
        onUpdate(() => result.trip)
      }
      if (result.syncState !== state) {
        await persistSyncState(trip.id, result.syncState)
        setSyncStateValue(result.syncState)
      }
    } catch {
      // offline o errore di rete: l'indicatore di stato lo segnala già
    }
  }

  useEffect(() => {
    let cancelled = false
    getSyncState(trip.id).then(async (state) => {
      if (cancelled) return
      setSyncStateValue(state)
      if (state) {
        const name = await getDisplayNamePreference()
        if (!cancelled) setCloudDisplayName(name)
        runSync(state)
      }
    })
    return () => { cancelled = true }
  }, [trip.id])

  useEffect(() => {
    function onOnline() {
      if (syncState) runSync(syncState)
    }
    window.addEventListener('online', onOnline)
    return () => window.removeEventListener('online', onOnline)
  }, [syncState])

  function handleUpdate(updater) {
    onUpdate(updater)
    if (syncState) {
      markDirty(trip.id)
      setSyncStateValue((s) => (s ? { ...s, dirty: true } : s))
    }
  }

  async function handleActivated(state) {
    await persistSyncState(trip.id, state)
    setSyncStateValue(state)
    const name = await getDisplayNamePreference()
    setCloudDisplayName(name)
  }

  async function handleRestore() {
    const restored = await restoreLastVersion(syncState.remoteId)
    if (!restored) {
      window.alert('Non c\'è nessuna versione precedente da ripristinare.')
      return
    }
    onUpdate(() => restored)
    const nextState = { ...syncState, dirty: false }
    await persistSyncState(trip.id, nextState)
    setSyncStateValue(nextState)
  }

  async function keepLocalVersion() {
    const result = await pushTrip(trip, syncState)
    await persistSyncState(trip.id, result.syncState)
    setSyncStateValue(result.syncState)
    setConflict(null)
  }

  async function keepOnlineVersion() {
    const result = await pullTrip(syncState)
    onUpdate(() => result.trip)
    await persistSyncState(trip.id, result.syncState)
    setSyncStateValue(result.syncState)
    setConflict(null)
  }

  function syncStatus() {
    if (!syncState) return null
    if (conflict) return { dot: 'bg-[var(--accent)]', label: 'due versioni in conflitto' }
    if (typeof navigator !== 'undefined' && !navigator.onLine) return { dot: 'bg-[var(--muted)]', label: 'in attesa di rete' }
    if (syncState.dirty && syncState.role === 'viewer') return { dot: 'bg-[var(--accent)]', label: 'modifiche salvate solo su questo telefono' }
    if (syncState.dirty) return { dot: 'bg-[var(--accent)]', label: 'modifiche in coda' }
    return { dot: 'bg-[var(--accent2)]', label: 'sincronizzato' }
  }

  const status = syncStatus()
```

- [ ] **Step 2: Mostra l'indicatore di stato nell'header**

Nel blocco dell'header, dopo il paragrafo `{trip.place && ...}`, aggiungi:

```jsx
          {status && (
            <div className="flex items-center gap-1.5 mt-1">
              <span className={`h-2 w-2 rounded-full ${status.dot}`} />
              <span className="text-xs text-[var(--muted)]">{status.label}</span>
            </div>
          )}
```

- [ ] **Step 3: Passa le nuove prop a `Overview`, `Days`, `Section` e aggiungi i modali**

Sostituisci il blocco `<main>` esistente:

```jsx
      <main className="px-5 max-w-2xl mx-auto pb-16">
        {activeTab === 'overview' && (
          <Overview
            trip={trip}
            onUpdate={handleUpdate}
            onDelete={onDelete}
            syncActive={!!syncState}
            onOpenActivate={() => setActivateOpen(true)}
            onRestore={syncState && syncState.role === 'editor' ? handleRestore : null}
          />
        )}
        {activeTab === 'days' && <Days trip={trip} onUpdate={handleUpdate} activeDisplayName={cloudDisplayName} />}
        {trip.sections.map((section) => (activeTab === section.id ? <Section key={section.id} trip={trip} section={section} onUpdate={handleUpdate} activeDisplayName={cloudDisplayName} /> : null))}
      </main>

      <ActivateSyncModal open={activateOpen} trip={trip} onClose={() => setActivateOpen(false)} onActivated={handleActivated} />

      <Modal open={!!conflict} title="Due versioni diverse" onClose={() => {}}>
        {conflict && (
          <div className="flex flex-col gap-3">
            <p className="text-sm">
              Questo viaggio è stato modificato sia su questo telefono che online, dopo l'ultima volta che si sono
              sincronizzati. Quale versione vuoi tenere?
            </p>
            <Btn onClick={keepLocalVersion}>Tieni la versione su questo telefono</Btn>
            <Btn variant="secondary" onClick={keepOnlineVersion}>Tieni la versione online</Btn>
          </div>
        )}
      </Modal>
```

- [ ] **Step 4: Aggiungi l'azione "Attiva sync" / "Ripristina l'ultima versione" in `Overview.jsx`**

Modifica la firma di `Overview` per accettare le nuove prop:

```jsx
export default function Overview({ trip, onUpdate, onDelete, syncActive, onOpenActivate, onRestore }) {
```

Nel blocco dei bottoni azione (dove ci sono già "Modifica", "Esporta", "Elimina viaggio"), aggiungi prima di "Elimina viaggio":

```jsx
        {!syncActive && (
          <Btn variant="secondary" onClick={onOpenActivate}>
            <Share2 size={16} /> Attiva sync
          </Btn>
        )}
        {onRestore && (
          <Btn variant="secondary" onClick={onRestore}>
            <Share2 size={16} /> Ripristina l'ultima versione
          </Btn>
        )}
```

- [ ] **Step 5: Verifica manuale**

Avvia `npm run dev`, apri un viaggio: deve comparire il bottone "Attiva sync" in Panoramica e nessun indicatore di stato nell'header (viaggio non attivo). Verifica che l'app funzioni ancora offline come prima (nessuna regressione sui viaggi non attivati).

- [ ] **Step 6: Commit**

```bash
git add src/views/TripView.jsx src/views/Overview.jsx
git commit -m "collega attivazione sync, indicatore di stato e conflitti in TripView"
```

---

### Task 14: Presenza e attribuzione sulle voci in `Days.jsx` e `Section.jsx`

**Files:**
- Modify: `src/views/Days.jsx`
- Modify: `src/views/Section.jsx`

**Interfaces:**
- Consumes: `stampModified` da `../data/schema.js`; `ModifiedBy` da `../components/ModifiedBy.jsx`.
- Produces: ogni giorno, voce di giorno, scheda, voce di checklist e sezione note mostra "modificato da" quando presente, e la aggiorna a ogni modifica se `activeDisplayName` non è vuota.

- [ ] **Step 1: `Days.jsx` — importa gli helper e la prop**

In cima al file:

```jsx
import { stampModified } from '../data/schema.js'
import ModifiedBy from '../components/ModifiedBy.jsx'
```

Cambia la firma: `export default function Days({ trip, onUpdate, activeDisplayName }) {`

- [ ] **Step 2: Applica `stampModified` in `saveDay` e `saveItem`**

Sostituisci `saveDay`:

```jsx
  function saveDay(e) {
    e.preventDefault()
    onUpdate((t) => {
      if (dayForm.id) {
        return { ...t, days: t.days.map((d) => (d.id === dayForm.id ? stampModified({ ...d, ...dayForm }, activeDisplayName) : d)) }
      }
      const day = stampModified({ id: crypto.randomUUID(), items: [], ...dayForm }, activeDisplayName)
      return { ...t, days: [...t.days, day].sort((a, b) => a.date.localeCompare(b.date)) }
    })
    setDayForm(null)
  }
```

Sostituisci `saveItem`:

```jsx
  function saveItem(e) {
    e.preventDefault()
    const { dayId, id, ...fields } = itemForm
    onUpdate((t) => ({
      ...t,
      days: t.days.map((d) => {
        if (d.id !== dayId) return d
        if (id) return { ...d, items: d.items.map((it) => (it.id === id ? stampModified({ ...it, ...fields }, activeDisplayName) : it)) }
        return { ...d, items: [...d.items, stampModified({ id: crypto.randomUUID(), ...fields }, activeDisplayName)] }
      })
    }))
    setItemForm(null)
  }
```

- [ ] **Step 3: Mostra `ModifiedBy` sotto il titolo del giorno e sotto ogni voce**

Dopo `{day.note && <p ...>{day.note}</p>}`, aggiungi:

```jsx
              <ModifiedBy modifiedBy={day.modifiedBy} modifiedAt={day.modifiedAt} />
```

Dentro il rendering di ogni voce, dopo il blocco del link (`{item.link && (...)}`), aggiungi:

```jsx
                    <ModifiedBy modifiedBy={item.modifiedBy} modifiedAt={item.modifiedAt} />
```

- [ ] **Step 4: `Section.jsx` — stesso trattamento per schede, checklist e note**

In cima al file:

```jsx
import { stampModified } from '../data/schema.js'
import ModifiedBy from '../components/ModifiedBy.jsx'
```

Cambia la firma: `export default function Section({ trip, section, onUpdate, activeDisplayName }) {`

Sostituisci `saveCard`:

```jsx
  function saveCard(e) {
    e.preventDefault()
    const tags = cardForm.tags.split(',').map((x) => x.trim()).filter(Boolean)
    onUpdate((t) =>
      updateSection(t, section.id, (s) => {
        if (cardForm.id) {
          return { ...s, items: s.items.map((it) => (it.id === cardForm.id ? stampModified({ ...it, ...cardForm, tags }, activeDisplayName) : it)) }
        }
        return { ...s, items: [...s.items, stampModified({ id: crypto.randomUUID(), ...cardForm, tags }, activeDisplayName)] }
      })
    )
    setCardForm(null)
  }
```

Sostituisci `toggleChecklistItem`:

```jsx
  function toggleChecklistItem(item) {
    onUpdate((t) => updateSection(t, section.id, (s) => ({ ...s, items: s.items.map((it) => (it.id === item.id ? stampModified({ ...it, done: !it.done }, activeDisplayName) : it)) })))
  }
```

Sostituisci `saveNotes`:

```jsx
  function saveNotes() {
    onUpdate((t) => updateSection(t, section.id, (s) => stampModified({ ...s, text: notesDraft }, activeDisplayName)))
  }
```

- [ ] **Step 5: Mostra `ModifiedBy` su schede, checklist e note**

Nel rendering di ogni scheda, dopo il blocco dei tag (`{item.tags.length > 0 && (...)}`), aggiungi:

```jsx
                <ModifiedBy modifiedBy={item.modifiedBy} modifiedAt={item.modifiedAt} />
```

Nel rendering di ogni voce di checklist, dopo `<span className={...}>{item.text}</span>`, aggiungi (dentro lo stesso `<li>`, come riga separata sotto, quindi avvolgi testo e presenza in un contenitore verticale):

```jsx
                <div className="flex-1">
                  <span className={`text-sm ${item.done ? 'line-through text-[var(--muted)]' : ''}`}>{item.text}</span>
                  <ModifiedBy modifiedBy={item.modifiedBy} modifiedAt={item.modifiedAt} />
                </div>
```

(questo blocco sostituisce lo `<span className={...}>{item.text}</span>` esistente).

Sotto la `<textarea>` della sezione `notes`, aggiungi:

```jsx
          <ModifiedBy modifiedBy={section.modifiedBy} modifiedAt={section.modifiedAt} />
```

- [ ] **Step 6: Verifica manuale**

`npm run dev`: attiva la sync su un viaggio di test (richiede che Task 7 sia stato applicato e `.env.local` compilato), modifica una voce, verifica che non compaia ancora nulla localmente (l'attribuzione appare solo dopo un pull, cioè su un secondo dispositivo/account). Verifica soprattutto che un viaggio **non** attivato non mostri mai "modificato da" e continui a funzionare offline esattamente come prima.

- [ ] **Step 7: Commit**

```bash
git add src/views/Days.jsx src/views/Section.jsx
git commit -m "aggiungi la presenza per singola voce in giorni e sezioni"
```

---

### Task 15: Verifica finale end-to-end

**Files:** nessuna modifica — solo verifica.

- [ ] **Step 1: Build e test**

```bash
npm test
npm run build
```

Expected: entrambi passano senza errori.

- [ ] **Step 2: Verifica offline (come in Fase 0)**

```bash
npm run preview
```

DevTools → Application → Service Workers (attivo) → Network → Offline: l'app deve aprirsi e mostrare i viaggi esistenti.

- [ ] **Step 3: Checklist manuale della spec (sezione 11)**

- [ ] Attivare sync su un viaggio da un browser, verificare la riga creata su `tv_trips` dalla dashboard Supabase.
- [ ] Aprire il link `wego.app/j/<share_code>` (o l'equivalente locale `localhost:5173/j/<share_code>`) da un secondo browser/profilo, completare il join, verificare che il viaggio appaia con ruolo `viewer`.
- [ ] Come owner, modificare una voce; sul client viewer, riaprire il viaggio e verificare che compaia "modificato da" dopo il pull.
- [ ] Simulare un conflitto: disconnettere la rete su entrambi i client, modificare lo stesso viaggio su entrambi, tornare online su entrambi in sequenza — verificare che il secondo mostri la modale di conflitto invece di sovrascrivere in silenzio.
- [ ] Usare "Ripristina l'ultima versione" e verificare che funzioni una sola volta (il bottone non ha più nulla da ripristinare subito dopo).

- [ ] **Step 4: Commit finale (se sono emerse correzioni durante la verifica)**

```bash
git add -A
git commit -m "fase 1 cloud: correzioni emerse dalla verifica end-to-end"
```
